# electron-builder config. Produces unsigned .dmg / .msi / .AppImage from
# the electron-vite output in `out/`. Signing + notarization are off by
# default — wire your Apple/Windows certs separately before publishing.

appId: app.octovault.desktop
productName: OctoVault
copyright: Copyright © 2026 OctoVault

directories:
  output: dist
  buildResources: build

files:
  - out/**/*
  - package.json
  - "!node_modules/**/*"

asar: true
asarUnpack: []

# Disable auto-publish; you upload manually.
publish: null

mac:
  category: public.app-category.productivity
  target:
    - target: dmg
      arch:
        - arm64
        - x64
  # Ad-hoc signed dev build (identity "-" produces a signature locally).
  # Combined with `xattr -dr com.apple.quarantine` on the .app after install,
  # this resolves the "OctoVault is damaged" Gatekeeper message. For a
  # public release that doesn't need the xattr step, replace with a real
  # Apple Developer ID and notarize.
  identity: "-"
  hardenedRuntime: false
  gatekeeperAssess: false
  icon: build/icon.png

dmg:
  title: ${productName}
  background: build/background.png
  # Window layout for the drag-to-Applications view. The background image
  # has labels positioned to align with these item slots — keep them in
  # sync if you re-design the background.
  window:
    width: 540
    height: 380
  contents:
    # Step 1 row: drag OctoVault.app → Applications.
    - x: 130
      y: 170
      type: file
    - x: 410
      y: 170
      type: link
      path: /Applications
    # Step 2: clickable Gatekeeper unlock — removes com.apple.quarantine
    # so the user doesn't get the "OctoVault is damaged" warning.
    - x: 270
      y: 310
      type: file
      path: build/Install.command

win:
  target:
    - target: nsis
      arch:
        - x64
  # icon: build/icon.ico

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true

linux:
  target:
    - AppImage
  category: Office
  # icon: build/icon.png
